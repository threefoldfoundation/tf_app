export interface SupportStatus {
  online: boolean;
}

/**
 * Values are the same as the sass variable names in variables.scss
 */
export enum TileColor {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  DANGER = 'danger',
  LIGHT = 'light',
  ORANGE = 'orange',
  GREY = 'grey'
}
